import { FormatChronometerPipe } from './format-chronometer.pipe';

describe('FormatChronometerPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatChronometerPipe();
    expect(pipe).toBeTruthy();
  });
});
